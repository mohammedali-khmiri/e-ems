import React from "react";
import "./styles.scss";

const Footer = (props) => {
	return (
		<footer className="footer">
			<div className="wrap">footer</div>
		</footer>
	);
};

export default Footer;
